# Where to find .gpr files (folder list)
set(ADA_GPR_DIRS "@ADA_GPR_DIRS@")

# And, in dependent packages, add themselves for the future
list(APPEND ADA_GPR_DIRS ${CMAKE_INSTALL_PREFIX}/share/gpr)
list(REMOVE_DUPLICATES ADA_GPR_DIRS)




